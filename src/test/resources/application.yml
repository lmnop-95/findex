spring:
  profiles:
    active: test

  datasource:
    url: jdbc:postgresql://localhost:5432/findex
    username: ${POSTGRESQL_USERNAME:commicat}
    password: ${POSTGRESQL_PASSWORD:1234}

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update

findex:
  openapi:
    base-url: https://apis.data.go.kr/1160100/service
    service-key: ${OPENAPI_SERVICE_KEY:}
